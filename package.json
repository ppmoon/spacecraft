{
  "name": "spacecraft",
  "version": "0.1.0",
  "description": "Extensible desktop workbench host (Tauri)",
  "private": true,
  "scripts": {
    "test": "cargo test --manifest-path src-tauri/Cargo.toml",
    "tauri": "tauri",
    "dev": "tauri dev",
    "start": "tauri dev",
    "build": "tauri build",
    "typecheck": "cargo check --manifest-path src-tauri/Cargo.toml",
    "smoke": "SPACECRAFT_SMOKE=1 tauri dev"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ppmoon/spacecraft.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
